'use client';

import WebDeveloperPage from '@/components/webdev/WebDeveloperPage';

export default function DevPage() {
  return (
    <div className="min-h-screen" style={{ background: 'var(--background)' }}>
      <WebDeveloperPage />
    </div>
  );
}